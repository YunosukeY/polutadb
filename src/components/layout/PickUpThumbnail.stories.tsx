import React from 'react';

import PickUpThumbnail from './PickUpThumbnail';
import { Singing } from '../../data/interfaces';

export default {
  title: 'layout/PickUpThumbnail',
};

export const showPickUpThumbnail = () => <PickUpThumbnail id='LQ_eazT56FA' singing={new Singing(0, 27, 0, 0)} />;
